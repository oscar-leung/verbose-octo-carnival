# PythonAnywhere subset of requirements.
# Drops:
#   - pyautogui            (needs display; we use cookie-mode + headless on PA)
#   - undetected-chromedriver (PA has no working Chromium build for UC; we
#                              fall through to the script's `init_driver_headless`
#                              path which uses plain selenium.webdriver.Chrome)
# These two are only used by local launchd runs and by export_*_cookies.py
# (which runs on your Mac, not on PA), so PA never imports them at runtime
# as long as you always pass --cookie-file.

requests>=2.25.1
beautifulsoup4>=4.9.3
lxml>=4.6.3
selenium>=4.18.0
python-dotenv>=1.0.0
gspread>=6.0.0
google-auth>=2.28.0
