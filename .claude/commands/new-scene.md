Delegate to the content-writer agent: create a new annotated Serifu
practice scene based on the request in $ARGUMENTS (a moment, theme, or
grammar target). The agent must follow .claude/agents/content-writer.md
and the conventions in serifu/client/src/data/demoScenes.ts, register the
scene in DEMO_SCENES (+ PUBLIC_SCENES if it deserves a public URL), and
leave typecheck/tests/build green. Then summarize: title, line count,
grammar patterns taught, and the public URL slug if any.
