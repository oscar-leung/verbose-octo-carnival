export const SAMPLES = {
  single: 'write Playwright tests',
  three: ['buy milk', 'write tests', 'ship it'] as const,
  long: 'an intentionally long todo item description that should still render on one line and truncate gracefully',
} as const;
